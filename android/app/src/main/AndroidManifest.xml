<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Google Play Compliance - Essential Context-Driven Permissions Only -->

    <!-- Crucial for AI Tutor API calls & connecting to backend -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 1. CAMERA: Required strictly for scanning homework, notes, and documents -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <!-- 2. RECORD_AUDIO: Required strictly for the AI voice tutor feature -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Required for WebView to properly route audio and trigger OS mic dialog -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <!-- 3. POST_NOTIFICATIONS: Required strictly for sending daily study reminder streaks (Android 13+ / API 33+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 4. STORAGE: Required strictly for file picker — PDFs and images (Android 13+ split permissions) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true">

        <!-- Disable Crashlytics auto-init: Crashlytics Gradle plugin not configured.
             Without the plugin, Crashlytics crashes the app on startup with a missing build ID error.
             This meta-data tag safely disables auto-initialization. -->
        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/AppTheme.NoActionBarLaunch">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          FileProvider: Required by Capacitor Camera plugin on Android 7+ (API 24+).
          Without this, camera cannot write temp files → causes "unable to create photo on disk".
          The authorities MUST match: "${applicationId}.fileprovider"
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>
</manifest>
